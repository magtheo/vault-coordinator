# Tasks
<!-- synced from Obsidian Vault — edit here or in Obsidian -->

- [x] V-001 Install and verify Vikunja (Docker, :3456) *(audit Aug 24: live, 200 OK, token-wired)*
- [x] V-002 Install and verify Radicale (Docker, :5232, two calendars) *(audit Aug 24: live, auth redirect OK)*
- [x] V-003 Configure DAVx⁵ on Android + identity round-trip test *(audit Aug 24: superseded — Kompakt app renders calendar via /v1/today, phone E2E passed Aug 23; DAVx⁵ path dropped)*
- [x] V-004 Install and verify ntfy (Docker, :8090) *(audit Aug 24: live, publishing V-057 agent alerts)*
- [x] V-005 Set up repo, venv, config.yaml, FastAPI skeleton *(audit Aug 24: systemd :8650)*
- [x] V-006 SQLite schema (entities, relationships, repo_snapshots, sync_state, notifications) *(audit Aug 24: schema.sql live, evolved through V-057)*
- [x] V-007 systemd service + Tailscale exposure *(audit Aug 24: systemd --user unit; phone reached it via tailnet Aug 23)*
- [x] V-008 Entity CRUD + alias resolution *(audit Aug 24: in routers, suite green)*
- [x] V-009 Relationship CRUD (create, query, tombstone, health check) *(audit Aug 24: in routers, suite green)*
- [x] V-010 get_relationship_by_event_uid *(audit Aug 24: event_uid in models/reminders)*
- [x] V-011 Vikunja adapter (read tasks, sync to entities) *(audit Aug 24: src/adapters/vikunja.py)*
- [x] V-012 Radicale adapter (create/read/update/delete CalDAV events) *(audit Aug 24: src/adapters/radicale.py)*
- [x] V-013 Repository adapter (parse TASKS.md + record provenance) *(audit Aug 24: src/adapters/repos.py)*
- [x] V-014 Idempotent POST /api/schedule endpoint *(audit Aug 24: src/routers/schedule.py)*
- [x] V-015 React/TypeScript + Vite setup *(audit Aug 24: frontend/, SPA served at :8650 root)*
- [x] V-016 Task list screen with sync indicators *(audit Aug 24: TaskList.tsx)*
- [x] V-017 Schedule bottom sheet (start + duration, 4 taps) *(audit Aug 24: ScheduleSheet.tsx)*
- [x] V-018 Today's schedule screen *(audit Aug 24: TodaySchedule.tsx)*
- [x] V-019 ntfy reminder worker (schedule, reschedule, cancel) *(audit Aug 24: src/reminders.py, APScheduler)*
- [x] V-020 Rebuild scheduler from DB on restart *(audit Aug 24: lifespan rebuild_scheduler_from_db)*
- [x] V-021 Run all 12 integration tests *(audit Aug 24: superseded — absorbed into the evolving suite, now 171 checks, all green)*
- [x] V-022 machine-status script + forced-command allowlist (machine repo, ADR 009)
- [x] V-023 Bearer-token auth middleware + CORS lockdown (config auth_token)
- [x] V-024 Machines adapter (localhost subprocess + SSH pull) + /api/machines
- [x] V-025 POST /machines/{name}/jobs/{job}/stop via allowlist
- [x] V-026 PWA Machines screen (live jobs + sessions, stop button, 30s poll, token prompt)
- [x] V-027 tailscale serve :8650 (tailnet-only HTTPS)
- [x] V-028 perf: background machines cache (30s poll, /api/machines 365ms → 2ms)
- [x] V-029 perf: TanStack Query client cache + stale-while-revalidate + all-tab prefetch
- [x] V-030 POST /api/machines/refresh (pull-to-refresh path)
- [x] V-031 machine-projects adapter (projects.toml parse, server clone)
- [x] V-032 GET /api/projects/overview — joined view (tasks+sessions+jobs per project)
- [x] V-033 PWA Projects tab (adaptive cards + detail sheet, cached + prefetched)
- [x] V-034 machine-status v3: panes, job-log, git-info (path-validated) verbs
- [x] V-035 machines cache enriched with per-project git info
- [x] V-036 GET /machines/{m}/jobs/{j}/log (tail, 5-200 lines)
- [x] V-037 overview v2: attention model (failed jobs/overdue/host-down), git chips, session panes, overdue tasks
- [x] V-038 GET /api/attention (Today strip)
- [x] V-039 PWA Projects v2: AI insights card (advisory-only glm-5.1, cached 10m), inline task complete, log modal, session pane expand, attention sorting
- [x] V-040 Today tab attention strip
- [x] V-041 curated run-commands: machine-status run verb (per-host registry match, command text never crosses the wire)
- [x] V-042 POST /machines/{m}/projects/{p}/commands/{k}/run
- [x] V-043 overview exposes declared commands (host+key) for PWA Run buttons
- [x] V-044 security: SPA fallback path traversal fixed (arbitrary file read incl. config.yaml unauthenticated); token compare now constant-time
- [x] V-045 ICS subscription sync worker: Google Calendar secret-ICS feeds → Radicale replicas (hourly, content-hash fast path, DTSTAMP-normalized per-UID diff, confirm-debounce vs Google export non-determinism, deletion mirror + mass-delete guard, ntfy failure alerts, drift heal every 24 runs); fixed ntfy credentials missing in config.yaml (reminders were 401ing silently)
- [x] W-001 A0 vault hygiene: baseline commit, scratchpad.md stub + README convention (two-writer discipline documented)
- [x] W-002 A0 vault adapter (src/adapters/vault.py): slugify w/ overrides (Hermes Dual-Bot→hermes), project scan, scratchpad read/append w/ scoped git commits
- [x] W-003 A0 vault config section (VaultConfig: root, git identity)
- [x] W-004 A0 endpoints: GET/POST /api/scratchpad, GET /api/projects/registry (vault ∪ projects.toml, vault wins, code dupes collapse), GET /api/labels, POST /api/labels/seed (idempotent)
- [x] W-005 A0 acceptance: prepend order verified, 3 scoped commits + clean tree, registry 8 vault + 3 code (11 unique), 15 labels seeded + idempotent re-seed, 401 without token
- [x] W-006 A1 vikunja adapter: label support (create_task w/ label_ids via attach endpoint, attach_label, detach_label)
- [x] W-007 A1 tasks router: label_ids on create, labels enrichment on GET /tasks, PUT/DELETE /tasks/{alias}/labels/{id} w/ entity re-projection
- [x] W-008 A1 schedule: GET /schedule/range (from/to aliases), shared _parse_raw_events refactor
- [x] W-009 A1 PWA shell: Today/Tasks/Calendar/Projects tabs, Machines demoted to header fleet chip overlay, labels prefetch
- [x] W-010 A1 CaptureBox: #tag autocomplete (arrow/tab/enter keys), chips, inline #tag parsing, last-used default
- [x] W-011 A1 TaskList: Vikunja-only, grouped (Overdue/Today/This week/Later/No date), filter chips by label usage, quick complete; TaskDetail label editor; Today strict (Now/Next card, schedule, due tasks); CalendarView month grid + day agenda; ProjectsView tagged-tasks section
- [x] W-012 A1 hygiene: tsc --noEmit enforced in build (fixed 12 latent type errors incl. pre-existing); acceptance: labels 15, range 3 events Aug, capture+attach+detach+complete round-trip, tailnet PWA 200
- [x] W-013 A1.1 complete-with-undo: 5s grace (row stays, dimmed/strikethrough), floating Undo pill w/ countdown above tab bar, tap checked circle to undo; complete fires immediately (never lost), undo = reopen; hook shared by Today + Tasks
- [x] W-014 A1.2 fix captured tasks not appearing until refresh: capture now inserts the created task into the ["tasks"] cache directly from the POST response (instant, refetch-independent); removed SW runtimeCaching for /api/tasks + /api/schedule/today (stale-cache class of bugs; patterns were also dead-wrong vs full URLs)
- [x] W-015 A1.3 hardening: undo hook stores completion promise (undo awaits it — no complete/reopen race); lists keep in-grace rows across refetch; TaskDetail resolves live entity from cache by ref; scratchpad POST/GET off the event loop (asyncio.to_thread for git), heading/body caps (200/10k), request_id idempotency (replay verified: 1 commit)
- [x] V-046 /v1/ Kompakt read contract (T-004, commits 14353b6+9e41e05): capabilities + 9 read endpoints (tasks/projects/areas/notes/inbox/today/chats/agents/agent-runs), snake_case wire, repo_snapshots machine:project join fix; ledger line added retroactively in V-047 commit
- [x] V-047 Device enrollment + per-device tokens (T-005, Kompakt Phase 4): devices table (Ed25519 pubkey, status, trust_class, capabilities, sha256 token_hash), enroll/challenge/activate endpoints (decoy nonces anti-enumeration, 202 pending, device_revoked 401), admin approve/revoke/delete + scripts/devices.py CLI, dual-principal auth middleware (admin config token vs capability-scoped device tokens), capability guards on all /v1 reads; live e2e 15/15 + client smoke green
- [x] V-048 §9 fail-closed feature gating (T-006): require_feature guard on /v1/notes, /v1/chats, /v1/agents, /v1/agent-runs — flagged-off features answer 501 instead of serving empty 200s (drift caught by Kompakt LiveServerSmokeTest consistency check; capabilities flag and wire behavior can no longer disagree); localhost instance restarted, tailnet systemd instance needs sudo restart
- [x] V-049 Capture pipeline (T-007, Kompakt Phase 6): src/capture.py deterministic interpret (en+no dates/times/keywords, 38/38 tests), POST /v1/capture/interpret + /v1/capture/commit (task→Vikunja+entity cache, note→scratchpad; request_id idempotent replay via mutations table; capability-gated capture.interpret/capture.commit), /v1/changes now serves confirmed mutations (server revision, since cursor) instead of stub, offline_capture=true; client: RemoteCaptureRepository + PendingCaptureStore (durable JSON queue, atomic rename) + QueueingCaptureRepository (offline commit parks w/ same request id, success drains queue), flush on remote activation; live e2e: interpret/commit/replay/changes/read-back green, smoke artifacts reverted; localhost :8650 restarted — tailnet systemd instance still needs sudo restart
- [x] V-051 AgentBackend port sketch (Kompakt Phase 8 / w-2, D025): src/agents/port.py — domain-derived port frozen (BackendCapabilities, ExecutionKind RUN|SESSION, ExecutionState incl. IDLE, SteerOutcome-honest steer, AgentResult with commit/salvage-evidence interpretation, 10 ops); two adapter mapping stubs w/ VERIFIED/UNVERIFIED endpoint citations — Warren (w-1 evidence: sandboxed/atomic/spawn-only, NDJSON events, finalize_failed≠failure lesson) + OpenCode (live :14096: resumable sessions, /agent + /command w/ templates, trusted-lane unsandboxed); rule-of-two spread asserted by test; contract tests 9/9 plain-python (`.venv/bin/python -m tests.test_agents_port`), main.py untouched (not imported anywhere — zero runtime risk); wiring/config/schema//v1 routes = V-052+; spike items marked: opencode message/abort/event-SSE shapes, warren run-list/cancel/seq-param, project_ref→directory/prj mapping table
- [x] V-052 Agent backends implemented (Phase 8, stacked on V-051): live-verified adapters — Warren (warren.py: POST /runs spawn, GET /runs/{id} `state`-poll, /events?since=&limit= NDJSON seq-cursor, /cancel idempotent-alreadyTerminal, run-kind sandboxed atomic) + OpenCode (opencode.py: v2 POST /api/session w/ location directory-binding, v1 POST /session/{id}/message sync driver, background-turn tasks + per-session busy lock, /abort mid-turn partial, /api/session/{id}/event?after= durable evidence, /session/{id}/command command-access, session-kind resumable unsandboxed, `live_steering=False` honest — v2 steer admission NOT consumed by v1-driven turns on v1.14.31); agent_executions projections table+repo (history survives backend restarts/swaps, coordinator-side ids stable); registry (config→backends, run_/ses_ prefix inference, project_ref→warren-project/opencode-directory mapping); /v1 agents surface in routers/agents.py behind `agents` feature flag (GET /agents, /agents/commands, POST /agents/dispatch, GET /agent-runs merged live-over-stored, get/result/events/send/command/steer/cancel w/ 400/404/409/501 semantics); config agents: section + example; tests: adapters 36 + registry/projections 20 + routes 25 (plain-python, MockTransport — no network), all existing suites green (port 9/9, capture 38/38)
- [x] V-053 Remote E2E agents surface (Kompakt T-011) + Warren v0.18 wire-shape fix: live dispatch exposed mock drift — v0.18 wraps single-run responses as {"run":…} with camelCase fields (agentName/projectId, createdAt epoch-ms, endedAt ISO, no updatedAt); warren.py fixed (unwrap at dispatch/get/result, _ts() ms→ISO-Z, title = prompt first line 80ch, updated = ended|started|created), adapter mocks rebaked to live shapes + 3 new unwrap/ts assertions; localhost :8650 restarted with WARREN_API_TOKEN injected (config agents: section, localhost-only backends); full live lifecycle green: dispatch → run detail → 62 events → interpreted result SUCCEEDED+salvage (finalize_failed, w-1 lesson 3) → honest unsupported steer → merged /v1/agent-runs list across both backends; suite 131 green (adapters 39); Kompakt LiveServerSmokeTest gains T-011 agents live test (surface decode, honest caps, terminal events+result, unsupported steer, real opencode dispatch round-trip). **Close-out Aug 23: admin auth_token ROTATED (old value leaked to a transcript); dev-5f9d854ed542 granted agent.write (13 caps, token hash untouched); PHONE dispatch E2E PASSED (opencode build role → glm-5.2 "READY" round-trip rendered on-device).**
- [x] V-054 Chat truncate primitive (Kompakt T-013): POST /v1/chats/{id}/truncate {request_id, keep_through?} — deletes every message strictly after the anchor by the list ordering (created_at, id); keep_through=None empties the thread; destructive, no branch kept; idempotent per request_id (§11 replay), chat.write capability, §9 flag fail-closed, thread revision bumped. Single primitive — revert/edit/regenerate compose client-side as truncate+send. Tests: tests/test_chat_truncate.py (14 checks: mid/no-op/empty truncations, cross-chat + unknown anchor 404s, replay, revision, 501 flag-off); suite 145 green.
- [x] V-055 Chat auto-title (Kompakt T-013 follow-up): placeholder titles ("New chat"/"Untitled"/blank) replaced by a title derived from the first user message (first line, ≤48 chars, word-boundary ellipsis) on send — explicit titles never overwritten, replays title-stable. 9/9 tests; suite 154.
- [x] V-056 OpenCode resume hardening (Kompakt T-017): external sessions (created outside the coordinator, listed via the adapter) report `project_ref: ""` from the backend — normalized `"" → None` in opencode.py so registry mapping + agent_executions projections + /v1 wire all round-trip a clean null instead of an empty string (client treats "" as a distinct key → session wouldn't re-bind to its directory on resume). Regression test added to tests/test_agents_adapters.py (external-session listing asserts None, not ""). Adapter suite 41/41; full suite 156 checks green. Re-verified live against self-upgraded OpenCode v1.18.21 on :14096: session listing, external-session resume, busy-lock 409 all green after :8650 restart.
- [x] V-057 Close the agent loop (Kompakt T-018, Phase 10 slice): dispatch → pocket → pinged. `watched`/`episode` columns on agent_executions (armed on dispatch + send — one episode per user-initiated turn); `agent_run_alerts` table (id `alert:{exec}:{episode}`, unread flag); background watcher task in lifespan (config-gated: agents.enabled; `poll_seconds` default 5, `notify_ntfy` default true) polls armed runs via backend.get(), on settle (terminal for runs, IDLE for sessions) records an alert, pushes ntfy to the configured topic with the alert id as deterministic Message-ID (FAILED → high priority, no alert for CANCELLED), disarms; /v1/inbox + /v1/today attention merge unread alerts (source_type=agent_run, source_id=backend execution id); POST /v1/inbox/{alert_id}/read dismisses. Tests: watcher pass, episode/alert semantics, inbox merge + read, backends-down resilience. Implemented: src/agents/watcher.py (asyncio task in lifespan), arming in dispatch+send routes. Suite 171 checks green (156 + 15 watcher, incl. live-fire legacy-schema migration regression). **Live E2E Aug 24: dispatch ses_fcd56df29ffeOu5orXiArYMCc8 → idle <5 s → alert row + inbox item (source_type=agent_run, deep-linkable) + ntfy publish (server stats messages_published 8→9) → POST read 200 → inbox cleared, read=1.** Two live-caught migration bugs fixed: (1) schema script's new indexes reference watched/episode → the ALTERs must run BEFORE executescript or startup crashes on legacy DBs; (2) fresh-DB guard — only ALTER when PRAGMA table_info returns rows (empty set = table not created yet; executescript's CREATE TABLE carries the columns).
- [x] V-058 Alert stream SSE (Kompakt T-019): GET /v1/alerts/stream — the app becomes its own push client (D008: foreground SSE in-app transport, no FCM; ntfy demoted to secondary channel). On connect replays ALL unread alerts (reconnect-safe; client dedupes by id), then live events; 15s heartbeat, Last-Event-ID honored, agent.read capability + feature-flag fail-closed (501). src/agents/alertbus.py: in-process asyncio.Queue fan-out, loop-safe cross-thread publish (call_soon_threadsafe), shared alert→item wire mapper (_agent_alert_items refactored onto it — one wire shape for inbox + stream); watcher publishes to the bus right after record_alert; projections.get_alert(). Tests: tests/test_alert_stream.py 19 checks — bus same-loop/cross-thread, wire shape, endpoint replay/live/dedupe/heartbeat/gate; endpoint legs run against a REAL uvicorn on an ephemeral port (TestClient/httpx-ASGITransport buffer infinite response bodies on httpx 0.28.1 — SSE headers never arrive). Suite 190 green (171+19). **Live E2E Aug 24: connect → 2 unread replayed instantly; dispatch ses_fccd0458dffeyE7wUT7RwvSza3 → live SSE event ~6s after dispatch (5s = watcher poll); result "pong"; run-detail + result endpoints verified.**
- [x] V-059 Server-side STT (Kompakt T-021, Phase 11 server leg): POST /v1/voice/transcribe — multipart clip upload → faster-whisper CPU int8 (Pascal GPU not worth the CT2 CUDA fight; `small` = Norwegian quality floor, beam_size=1 + VAD for snappy short clips) → `{text, language, duration_s}`. Explicit-recording contract (dev plan §13): one clip per user tap; temp file deleted in a finally block on success AND failure — audio never survives the request; transcript becomes task/note/chat only via explicit user transitions. Gate order: `voice.transcribe` capability → `voice_transcription` feature flag (501 fail-closed, protocol §9) → content-type whitelist audio/* + application/ogg (415) → streamed byte cap 25 MiB (413; read max+1, never buffers past limit) → av container probe: duration cap 120 s (413) + decode check (415 on garbage) → whisper ValueError → 400 (e.g. unknown language code). Language precedence: request param (normalized `"NO "`→`no`; `""`→auto) → config `default_language` → auto-detect. src/voice.py: process-wide serialized singleton (lock — WhisperModel not documented thread-safe), config-fingerprinted lazy reload, `reset_model()` test hook; transcription in the threadpool (`run_in_threadpool` — CPU work off the event loop). Config: `voice:` section (model_size/device/compute_type/cpu_threads/default_language/max_upload_bytes/max_duration_s) — defaults work with zero config.yaml changes. `voice.transcribe` added to DEFAULT_DEVICE_CAPABILITIES (existing devices get it at T-021 time via admin approve with payload rebuilt from the DB row). Tests: tests/test_voice.py 19 checks — real multipart, engine patched at router namespace (bytes-exact handoff asserted), temp-file lifecycle incl. deletion on engine failure, every error code, capability gate via stamped device principal, 501 flag-off, capabilities-wire advertisement. Suite **209 green** (190+19). **Live E2E Aug 24: 3 s tone.wav → 200 `{"text":"","language":"no","duration_s":3.0}` in 1.2 s incl. model load; png → 415; garbage-bytes wav → 415; /tmp zero residue; `voice_transcription:true` + 14-agent fingerprint on the wire after systemctl --user restart.** Real Norwegian speech rides the T-021 phone leg (no TTS on the box for a synthetic test). requirements.txt: faster-whisper pinned.
- [x] V-060a Vault-wide notes core (Phase 12 / D028 v2 server leg, file-authoritative): src/notes.py — per-request vault walk of the PARA scope (NO index table: ~131 files → milliseconds, zero migration/staleness; a table would be a later optimization only). Ids `vault:note:{sha1(relpath)[:10]}` (opaque, paths never cross the wire, D023); `_resolve` is the only id→path mapping (walks + hash-compares — no client path injection, no traversal). Roles: scratchpad (root `scratchpad.md`, pinned first in list) / inbox (`00 - Inbox/…`) / note; `_join_ref` derives `vault:project:{slug}` / `vault:area:{slug}` from PARA placement (D004); title = first `# ` heading → frontmatter title → filename; preview = first non-heading line (120 chars); updated_at = mtime (no caching — text read at request time by design). Write path: `write_note` sha256-checksum optimistic lock (stale → ChecksumMismatch carrying the fresh note → 409 + fresh body; client reloads, nothing lost), pathspec-scoped git commit via existing vault-commit infra. `create_note_file`: `00 - Inbox/<slug>.md` + minimal frontmatter (`captured:`, `source:`), collision → `-2` suffix, idempotent via mutations-ledger replay (request_id). Routes: GET /v1/notes (filters project_id/area_id/category, 200-row cap), GET /v1/notes/{id} (text+checksum), PUT /v1/notes/{id} (`note.write`; 422 empty/oversized), POST /v1/notes (`note.write`, source_type whitelist, replay returns original result + `replayed:true`). `FEATURES["notes"]=True` (was fail-closed stub). Config: `notes:` section (`include_dirs` PARA scope + `root_files` + `max_body_chars` 200k) — RepoTasks/Templates/README/attachments excluded by construction. `note.read` in DEFAULT_DEVICE_CAPABILITIES; phone device row already carries note.read+note.write (checked in DB — no re-approval needed). Tests: tests/test_notes.py **39 checks** on a real tmp git vault — scan boundaries (excludes/mtime ordering/scratchpad pin/id stability), detail freshness (external edit between calls), PUT happy/409-fresh-body/404/422×2, POST happy/replay/collision/422×2, filters, 501 flag-off, device capability gates (read ok / write 403 via stamped principal). Suite **248 green** (209+39). **Live E2E Aug 24 after systemctl --user restart: unauth /v1/status → 401; GET /v1/notes → 106 notes, scratchpad pinned, joins resolving (kodeverket/hermes/development); GET /v1/notes/vault:note:dbd719881c → 200 text+checksum.** (First live detail 404 was a red herring — my own debug print truncated the id at 20 chars; ids are 21.) Commits: c76a719 (branch), 000b39b (merge --no-ff → main, pushed).
- [x] V-062 Chat scopes — topic tier (T-022d server, plan docs/plans/2026-08-25-t022d-chat-scopes.md): three chat tiers land in chat_threads — `scope_type` NULL|'topic'|'workspace' + `scope_ref` (CHECK-constrained, both-set-or-both-null, ALTER-before-executescript migration + fresh-DB guard). Topics ARE the sorter's `notes.buckets` (single registry, no parallel list; empty → honest empty picker). src/chat_scopes.py: `chat_topics` (wire `{id,label}`), `propose_topic` (deterministic sorter matcher — alias-first-line 5 / kw-first-line 3 / kw-body 2, threshold 3; NEVER auto-applied, NEVER proposes workspaces — repos are an explicit user choice), `seeded_system_prompt` (DEFAULT_SYSTEM_PROMPT + `00 - Inbox/<key>.md` read at SEND time, 8000-char cap w/ truncation note, missing file → plain default = honest cold start), `validate_scope` (topic ∈ buckets, workspace ∈ live workspaces registry → 422 on miss), `scope_label` (dangling ref renders raw, never 500s a list). Wire: GET /v1/chat/topics (chat.read); POST /v1/chats gains optional scope pair (validated, old clients unaffected); POST /v1/chats/{id}/scope `{request_id, scope_type?, scope_ref?}` = the ONLY re-scoping path (chip Apply, picker change, un-scope; idempotent §11); send result gains `proposed_topic` ONLY on unscoped threads + matcher hit; `_wire_thread` gains scope_type/scope_ref/scope_label/pending_reply. Tests: tests/test_chat_scopes.py 28 checks — topics shape, create-with-scope ±422s, workspace validation via patched registry, seeded send (fake LLM captures cfg.system_prompt: bucket content + default base + user/assistant-only history), missing-file unseeded, propose hit/miss/scoped-silent, scope apply/clear/replay/bad-ref, legacy-row wire, pre-V-062 DB migration. Suite 276 green (248+28). Commits: 0f26701 (branch), merged 1d9deac. Live E2E verified Aug 25: /v1/chat/topics → 13 buckets; topic chat (evershift) assistant quoted the `00 - Inbox/evershift.md` seed verbatim ("greedy meshing pass on the voxel chunks before PSX rasterization") = send-time seeding confirmed, no proposed_topic on scoped thread; unscoped send w/ first-line alias → proposed_topic {evershift}, thread left unscoped (never auto-applied), chip Apply via /scope → scope_label "Evershift". Only evershift.md exists in 00 - Inbox today — other topics = honest cold start until capture/sorter writes them.
- [x] V-063 Chat scopes — workspace tier (T-022d server): workspace chats run on the OpenCode adapter DIRECTLY (chat ≠ agent — no agent_executions row, watcher/alert loop never fires for chats). chat_threads gains `agent_execution_id` (OpenCode session) + `pending_turn` (turn settled, reply unrecorded). src/chat_workspace.py: `wait_for_turn` (polls backend.get() — public truth, restart-surviving — until not-RUNNING or budget) + `auto_commit` (git add -A + `kompakt chat: <title>` after each recorded reply; clean-tree skip; any git failure logs + continues; None identity, no co-author trailers; never fails the request). v1.py `_workspace_send`: catch-up pending reply first (still-running → honest busy note, no racy concurrent send — restart edge guarded by placeholder-result check), first send dispatches (agent="general", project_ref=scope_ref; refs align with the registry's project_dirs by construction), later sends resume via backend.send (SessionBusyError → busy note, message persisted — mirrors LLM-failure policy); settle within `chat.workspace_timeout_s` (default 180) → reply = result().summary (2000-capped), pending_turn cleared, auto-commit; timeout → degraded note + pending_turn=1, backfilled on next send OR messages GET (catch-up at list time, listing never blocks/never fails). Send result carries `workspace: {state: settled|pending|busy|error|unavailable, execution_id?, committed?}` — additive, old clients ignore. Config: ChatConfig.workspace_timeout_s. Tests: tests/test_workspace_chats.py 28 checks — happy path (dispatch ref/agent asserted, reply recorded, execution id stored, real tmp git repo gets the `kompakt chat:` commit), resume-not-redispatch, clean-tree committed:false, timeout → pending + wire pending_reply + GET backfill, still-pending busy + SessionBusyError busy + user message persisted, backend-unavailable 200-note, non-repo dir never fails the send. Suite 304 green (276+28). Commits: 0f26701 (branch), merged 1d9deac. Live E2E verified Aug 25 vs workspace `roblox-toolkit` (local-only, no remote, clean tree): write turn dispatched a real OpenCode session (glm-5.2/zai-coding-plan), settled ~11 s, `workspace: {state: settled, committed: true}`, git received exactly `kompakt chat: E2E workspace tier` (1 file, 1 insertion — nothing else swept, tree clean); read-only turn RESUMED the same execution_id (no re-dispatch), recalled session memory correctly, `committed: false` clean-tree skip, HEAD untouched. Scratch commit reset afterward (repo restored). Gap noted: no chat delete/archive API yet — E2E threads ("E2E …" titled) remain as visible artifacts; deletion is client-leg backlog.
- [x] V-064 Project-target note creation (T-022e server leg, plan Kompakt-Interface docs/plans/2026-08-25-t022e-save-to-project.md): POST /v1/notes gains optional `project_id` — full `vault:project:{slug}` id only, resolved against live `list_vault_projects` (slug→FOLDER NAME — overrides honored server-side); note lands `02 - Projects/<name>/<yyyy-mm-dd>-<slug>.md` instead of `00 - Inbox/`, same frontmatter/unique-suffix/scoped-commit; wire row auto-derives `project_id` from the path (D028 join, zero extra plumbing — filters/source links work unchanged). Validation 422s BEFORE the mutation ledger records: unknown slug, non-vault prefix (machine: projects have no vault folder), bare slug. `create_note_file` gains `project_name` param; `_unique_inbox_path` generalized to `_unique_note_path(folder)` (single caller). Old clients unaffected (field optional); new clients on old servers degrade to inbox (unknown field ignored, D010). Tests +13 in tests/test_notes.py (**50 checks**: target/join/category=Projects/replay-idempotent/validation trio/project-filter integration). Suite 15/15 files green. Commits: 93d6ac6 (branch), merged 19e1774. **Live E2E Aug 25**: create with `vault:project:kodeverket` → `02 - Projects/KodeVerket/2026-08-25-v-064-live-probe.md` + scoped vault commit 4da602a, row joined project_id/category Projects; replay same request_id → replayed:true, same id, no second file; probe reverted (vault 79035c2). **Operational finding (restart took ~90 s)**: uvicorn graceful shutdown WAITS on the phone's long-lived SSE connection — "Waiting for connections to close" until systemd TimeoutStopSec SIGKILLs. Restart always succeeds but is slow while a device holds /v1/alerts/stream; fix candidate = lifespan shutdown cancelling SSE generators or TimeoutStopSec drop-in (backlog, not urgent).

